arr1 = [1, 2, 3, 4, 5] // Numbers
arr2 = ["Vancouver", "Toronto", "Calgary"] // Strings
arr3 = [1, "Vancouver", 3, true, "Toronto"] // It works

// add the last 
arr1.push(20);

console.log(arr1);

// remove the last
arr1.pop();

console.log(arr1);

// add the front
arr1.shift(5);

console.log(arr1);


// toArray({ a: 1, b: 2 }) → [["a":, 1], [&wuot;b&quot;, 2]]

// object.entries → return key and value

// object.values → return value

// object.keys → return key

// numbers.push(num * (i + 1));